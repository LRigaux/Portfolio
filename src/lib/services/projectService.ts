/**
 * Service pour la gestion des projets
 * Centralise la logique métier liée aux projets
 */
export async function getProjects(filters: ProjectFilters, pagination: Pagination) {
  // Logique de récupération des projets
}

export async function createProject(data: ProjectInput) {
  // Logique de création de projet
}

// etc.